#!/bin/sh
APIGEN="docker run --rm vfac/apigen"
if [ "${TRAVIS_BRANCH}" != "master" ]; then
  rm -rf docs/${TRAVIS_BRANCH}/*
  $APIGEN generate src/ --destination docs/${TRAVIS_BRANCH}
else
  rm docs/*
  rm -rf docs/*
  $APIGEN generate src/ --destination docs/
fi
